/* ============================================================================
 * Visibilidad temporal de módulos.
 *
 * Interruptor de UI únicamente: no toca la base de datos ni las banderas del
 * negocio. Poner en `true` vuelve a mostrar el módulo tal como estaba.
 * ========================================================================== */

/** Muestra u oculta todo lo relacionado con clientes y lealtad en la interfaz. */
export const SHOW_LEALTAD_UI: boolean = false;
